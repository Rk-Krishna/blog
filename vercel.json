{
    "version": 2,
    "builds": [
      {
        "src": "userend/asgi.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "userend/asgi.py"
      }
    ]
    "builds": [
    {
      "src": "your_project_name/asgi.py",
      "use": "@vercel/python",
      "config": {
        "install": "pip install -r requirements.txt"
      }
    }
  ]
  }
  
