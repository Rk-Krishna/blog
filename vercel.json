{
  "version": 2,
  "builds": [
    {
      "src": "userend/asgi.py",
      "use": "@vercel/python",
      "config": {
        "install": "pip install -r requirements.txt"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "userend/asgi.py"
    }
  ]
}
